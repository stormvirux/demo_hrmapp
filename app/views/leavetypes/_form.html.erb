<%= simple_form_for @leavetype, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :ltype %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                leavetypes_path, :class => 'btn' %>
  </div>
<% end %>
